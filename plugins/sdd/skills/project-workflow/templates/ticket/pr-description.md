<!-- PR Description Template
Variables substituted by /sdd:pr command:
- ${TICKET_ID}: Ticket identifier (e.g., SDDPR, UIT-3670)
- ${TICKET_NAME}: Kebab-case ticket name (e.g., add-pr-workflow-step)
- ${SUMMARY}: Brief summary from README.md or ticket name
- ${CHANGES}: High-level changes from plan.md phases
- ${JIRA_SECTION}: Conditional Jira link (empty if not a Jira ticket)
- ${TESTING}: Testing verification status

Note: PR title is generated separately as: [${TICKET_ID}] ${SUMMARY}
-->

## Summary

**Ticket:** ${TICKET_ID}

${SUMMARY}

${JIRA_SECTION}

## Changes

${CHANGES}

## Testing

${TESTING}

---

*Created with [SDD Workflow](/sdd:pr)*
