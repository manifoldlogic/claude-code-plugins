{
  "description": "Maproom indexing workflow reminders",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__maproom__search",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Reminder: Ensure repository is indexed before searching. Check with mcp__maproom__status or run a scan if needed.'"
          }
        ]
      }
    ]
  }
}
